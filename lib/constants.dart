import 'package:flutter/material.dart';

const defaultWallpaper = 'assets/sliver_banner_1.jpg';
const animationDuration = Duration(milliseconds: 200);
const roundedTopBorder = RoundedRectangleBorder(borderRadius: BorderRadius.vertical(top: Radius.circular(30.0)));
const defaultDivider = Divider(height: 0.0, indent: 15.0, endIndent: 15.0);
